to execute:  
create directory in jupyter notbook called "code\_and\_model"  
in "code\_and\_model" create directory called "testdata"  
upload the three files "model.pth", "test.ipynb", "train.ipynb" in the directory "code\_and\_model"  
in the "testdata" directory, upload the "import\_data.ipynb" file  
open the import data file, enter the address to the testdata.zip file, and then run the   import\_data file  
delete the import\_data file

if you do not have a .zip of the testdata simply take the three folders containing the test   data, select all three, right click "add to archive" and save as a .zip

